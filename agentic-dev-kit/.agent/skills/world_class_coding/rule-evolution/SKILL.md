---
name: rule-evolution
description: 专注 Agent 规则自身的清扫与打理。当执行 `/evolve` 时触发。
version: 1.0.0
---

# 规则进化协议 (Rule Evolution Protocol)

> 规则和技能不是静态的。它们必须随着项目和 Agent 能力的演进而迭代。本协议定义了规则从诞生到清理的完整生命周期。

## 一、进化循环

```
观察 Agent 不良行为
  → 添加一条针对性规则
    → 规则积累，开始互相矛盾或膨胀
      → 触发 "Spa Day" 整合清理
        → 恢复高效状态
          → 继续观察…（循环）
```

## 二、反馈捕获 — 何时添加新规则

当出现以下信号时，应新增规则：

- Agent 引入了你不喜欢的编码风格或模式
- Agent 重复犯同一类错误
- Agent 自行做出了你未授权的决策
- Agent 在特定场景下的行为不符合预期

**添加规则的原则**:
- 每次只添加**一条**针对性规则，不要一次加多条
- 规则必须具体、可执行（"禁止在循环中做数据库查询"），而非泛泛而谈（"写好代码"）
- 新规则添加后，在下一次使用中观察是否生效

## 三、退化信号 — 何时触发清理

当出现以下信号时，说明规则膨胀已影响 Agent 效能：

| 退化信号 | 说明 |
|---|---|
| Agent 开始忽略某些规则 | 规则太多，上下文顾不过来 |
| Agent 表现出矛盾行为 | 规则之间互相冲突 |
| Agent 启动变慢或响应迟缓 | 需要加载太多指令文件 |
| 规则超过 20 条 | 绝对数量警戒线 |
| 需要加载 >5 个 md 文件 | 文件数量警戒线 |
| 同一类规则出现在多个文件中 | 规则分散、重复 |

## 四、Spa Day — 规则整合清理流程

使用 `/evolve` 工作流触发，或每 2-4 周手动执行一次：

1. **盘点**: 读取所有 Rules、Skills、AGENT.md 中的条目
2. **分类**: 将所有规则按主题分组（编码规范、测试、安全、架构…）
3. **去重**: 合并语义相同但表述不同的规则
4. **冲突检测**: 识别互相矛盾的规则，向用户确认以哪条为准
5. **淘汰**: 移除已被 Agent 内置能力覆盖的过时规则
6. **重组**: 如果某个类别规则过多，拆分为独立文件并在 AGENT.md 中路由
7. **验证**: 清理后运行一个典型任务，确认 Agent 行为符合预期
